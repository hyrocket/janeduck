# JaneDuck 업무일지 — 2026-05-18

## 주요 진행

### Writing Mode 노드 전체 real 교체 + UI 완성 (`724ec03`)

#### LangGraph 백엔드 — stub → real

| 노드 | 구현 내용 |
|---|---|
| `load_context` | DB에서 card + user_card 로드, mastery/scaffold/attempt_count 반환 |
| `introduce_word` | writing_attempts_count == 0 첫 만남에만 LLM 인트로 생성, 이후 skip |
| `determine_scaffold` | ScaffoldDecider: mastery 0-1→high, 2-4→medium, 5→low |
| `prompt_high/medium/low` | 실제 LLM 프롬프트 생성 (try_again 시 재사용) |
| `check_target_word` | 정규식 + 어근 매칭 |
| `evaluate_writing` | gpt-4.1-mini, strict JSON schema |
| `update_srs_mastery` | MasteryUpdater + DB write (결정론적 UUID5 idempotent) |
| `present_feedback` | ActionSuggester §8-1 룰 |
| `await_user_action` | PAUSE 2 interrupt |

신규 모듈: `workflows/db.py`, `workflows/judgers.py`, `workflows/nodes/context.py`, `workflows/nodes/introduce.py`

**master_challenge mastery 승격 버그 수정:**
- 문제: try_again과 master_challenge가 동일하게 세션 초기 mastery 기준으로 계산
- 수정: `await_user_action_node` — master_challenge 선택 시 `mastery_level = mastery_level_after`(직전 attempt 결과)로 승격

#### Writing Mode UI 완성

- `IntroBubble`: 첫 만남 인트로 버블
- high scaffold: starter prefix 자동 미리 채우기 (useEffect)
- 헤더: 단어 + 정의 + scaffold 배지 + mastery dots (🎖️, opacity로 획득/미획득 구분)
- 작문 후 mastery 변경 시 dots 즉시 갱신
- next_word: sessionStorage 큐에서 다음 카드 자동 로드 (큐 소진 시 all_done 화면)

#### starter_templates 형식 재정의 + 99개 재생성

- 형식: 사람 주어(I/My friend/She 등) + 목표 단어 + when/after/because 뒤 빈칸 ___
- 금지: 단어 자체가 주어인 설명형 ("Resilience is important because ___")
- `scripts/regen_starters.py` 신규 작성 (___ 포함 여부 검증 포함)
- 99개 전체 재생성 완료, Neon DB 반영

---

### MemorySaver → AsyncPostgresSaver (Neon) 전환 (`36b1ac9`)

- Vercel serverless 환경에서 in-memory 상태 유지 불가 → Neon PostgreSQL checkpointer 도입
- `langgraph-checkpoint-postgres` 패키지 추가
- 초기: module-level async state 문제 발생 → per-request `from_conn_string` 방식으로 수정 (`3c76e75`)
- `studio_graph`(checkpointer 없음) / `writing_graph`(AsyncPostgresSaver) 이중 지원 유지

---

### Vercel Python Functions 배포 디버깅 및 완료

#### 문제 발생 순서 및 해결

| 순서 | 문제 | 해결 |
|---|---|---|
| 1 | `vercel.json` routing — `/api/py` prefix 매칭 오류 | rewrite destination `/api/index` → `/api` 수정 (`94b56b3`) |
| 2 | Mangum base_path 불일치 | `Mangum(app, lifespan="off", base_path="/api/py")` 설정 (`6b77823`) |
| 3 | `api/python/` 스켈레톤 충돌 | 구 `api/python/` 폴더 삭제 (`29f7e1a`) |
| 4 | lifespan 오류 (serverless 비호환) | lifespan 제거 → lazy init 방식으로 교체 (`1f80193`) |
| 5 | Mangum ASGI 호환성 오류 | Mangum → **a2wsgi** 교체 (`0428da4`) |
| 6 | NextAuth Vercel reverse proxy 401 | `trustHost: true` 추가 (`f2d763f`) |
| 7 | Python import 오류 (`ModuleNotFoundError`) | `api/` 디렉토리를 `sys.path`에 추가 (`8e3f286`) |
| 8 | diag 엔드포인트 제거 | `/api/diag` 제거 (`a3f649f`) |

**최종 결과:** Vercel 배포 정상 — Python Functions, NextAuth, Writing Mode API 모두 동작 확인 ✅

---

### UI 폴리시 + 기타

- scaffold 표시명: high→"Guided Mode", medium→"Practice Mode", low→"Challenge Mode" (`299ef89`)
- writing_rating 표시명: again→"Keep practicing", hard→"Getting there", good→"Well done", easy→"Excellent"
- master_challenge 버튼: 고정 라벨 → 다음 scaffold 모드명 동적 표시 ("Try Practice Mode →" / "Try Challenge Mode →")
- DuckAvatar: 🦆 이모지 → `/public/duck-avatar.png` 이미지 (44px)
- decks 페이지: "별표 카드" → "Starred"
- 비로그인 Writing Mode 접근 허용 + save-reminder 토스트 (`31e0a25`)
- 로그인 태그라인 업데이트 (`d004cf4`)

---

## 완료 항목 (Phase 0~3 전체 완료)

- [x] SRS_SPEC.md 작성 + lib/srs/update.ts 재검토 완료
- [x] MemorySaver → AsyncPostgresSaver (Neon checkpointer) 전환
- [x] Vercel 배포 검증 완료 (FastAPI 307MB / 500MB 한도 이내)
- [x] Google OAuth trustHost 설정 완료

---

## 남은 작업 (Phase 4~5)

- [ ] 대시보드 (학습 진도)
- [ ] 세션 시작 화면 (Deck/모드 선택)
- [ ] PWA 설정 (manifest, service worker)
- [ ] 모바일/태블릿 UI 폴리시
- [ ] 실제 사용자 테스트

---

## 주요 결정사항

| 항목 | 결정 | 이유 |
|---|---|---|
| ASGI 어댑터 | Mangum → **a2wsgi** | Vercel Python runtime에서 Mangum ASGI 호환 오류 지속 발생 |
| checkpointer | MemorySaver → AsyncPostgresSaver | Vercel serverless — 프로세스 간 상태 공유 불가 |
| Python import | `sys.path.insert(0, api_dir)` | Vercel Functions 번들 경로에서 상대 임포트 불가 |
| scaffold 표시명 | high/medium/low → Guided/Practice/Challenge Mode | 학생 친화적 레이블 |
| idempotency | UUID5(card_id|user_id|session_id|attempt_count|user_text) | 동일 요청 중복 DB write 방지 |
