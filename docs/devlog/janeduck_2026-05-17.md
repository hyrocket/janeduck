# JaneDuck 업무일지 — 2026-05-17

## 주요 진행

### 미커밋 변경사항 정리
- `CLAUDE.md`: LangGraph 워크플로우 전략 섹션 추가 커밋
- `scripts/migrate.ts`: `splitStatements()` `$$` 달러쿼트 버그픽스 커밋

### AI Provider 전환 — Anthropic → OpenAI
- `langchain-anthropic` → `langchain-openai==0.3.16`
- 모델: `gpt-4o-mini` (최소 토큰 비용)
- 변경 파일: `api/python/requirements.txt`, `api/requirements.txt`, `api/python/health.py`, `.env.local`
- `.env.local`: `ANTHROPIC_API_KEY` → `OPENAI_API_KEY` + `OPENAI_MODEL=gpt-4o-mini`
- `CLAUDE.md` AI 스택 표기 업데이트

### DESIGN_DECISIONS.md 검토 및 DB 스키마 v2 마이그레이션
- `DESIGN_DECISIONS.md` 신규 작성 (동작 명세 문서) 검토 완료
- **스키마 변경 원칙 확정:** §12는 최소 명세. 명시적 금지 항목만 삭제, 나머지 기존 필드 유지.

#### schema.sql v2 주요 변경
| 테이블 | 변경 내용 |
|---|---|
| `cards` | `term` → `word`, `difficulty` → `difficulty_band` (common/uncommon/advanced), `starter_templates` + `topic_hints` 추가 |
| `user_cards` | `mastery_level` 범위 1~4 → **0~5**, `writing_count` → `writing_attempts_count`, `last_self_eval_rating`, `recent_scores`, `current_scaffold`, `is_starred`, `last_writing_score` 추가 |
| `writing_attempts` | `scaffold_level` → `scaffold_used`, `user_sentence` → `user_text`, `ai_feedback` JSONB → TEXT, `session_id`, `is_master_challenge`, `topic_used`, `structure_guide_used`, `ai_strengths`, `ai_weakness_signals`, `writing_rating` 추가 |
| `study_sessions` | `session_type` (mixed/starred_only/weak_only) 추가 |

#### 마이그레이션 실행
- `lib/db/migrations/001_update_schema.sql` — 1차 (ADD COLUMN, DROP/ADD 위주)
- `lib/db/migrations/002_remaining_renames.sql` — 2차 (RENAME, 타입 변경, constraint 수정)
- `scripts/apply_migration.ts` — 마이그레이션 러너 신규 작성
- **버그 수정:** 주석 뒤 첫 SQL 구문이 필터링되던 문제 → comment strip 로직 수정

#### lib/types.ts 업데이트
- 신규 타입: `SessionType`, `SelfEvalRating`, `DifficultyBand`
- `Card.term` → `word`, `difficulty` → `difficulty_band`, `starter_templates`, `topic_hints` 추가
- `UserCard` 전면 개편 (mastery 0~5, self_eval 신호 분리, scaffold/star 추가)
- `WritingAttempt` 전면 개편 (scaffold_used, user_text, ai_feedback TEXT 등)
- `StudySession.session_type` 추가

### Phase 1 — 단어 큐레이션 및 시드 데이터
- 단어 소스: Hoon이 직접 제공 (Top 100 Sec 2 Advanced 단어 리스트)
- 중복 제거: Sustainability ×2, Sustainable Development ×1 → **99개 확정**
- `data/deck_sec2_advanced.json` 작성 (99개 카드, 4개 테마)
  - Emotional Intelligence & Mental Health: 25개
  - Environmental Awareness & Action: 24개 (중복 1개 제거)
  - Digital Literacy & Media: 25개
  - Global Citizenship & Social Responsibility: 25개 (중복 2개 제거)
- `scripts/seed_deck.ts` 작성 + `npm run db:seed` 명령 추가
- **Neon DB 삽입 완료** — Deck id: `e5e400a8`, 99 cards

---

## 남은 작업

### 즉시 가능
- [ ] OpenAI API 키 입력 → `.env.local`
- [ ] Google OAuth credentials 입력

### API 키 입력 후
- [ ] `starter_templates` + `topic_hints` 배치 생성 스크립트 (OpenAI, 99개)
- [ ] Vercel 환경변수 설정 (DATABASE_URL, AUTH_SECRET, OPENAI_API_KEY 등)

### Phase 2 (UI 개발)
- [ ] Mode 1: Quick Review 플래시카드 UI
- [ ] Mode 2: Writing Mode UI (scaffold_level 기반)

---

## 주요 결정사항

| 항목 | 결정 | 이유 |
|------|------|------|
| AI Provider | Anthropic → **OpenAI gpt-4o-mini** | 기존 API 키 보유, 최소 비용 |
| 단어 소스 | Hoon 직접 제공 | Singapore 학생 실정에 맞는 단어는 본인이 가장 잘 앎 |
| Deck 구조 | 단일 Deck "Sec 2 Advanced (test)" | 99개로 카드 20장 로딩 테스트에 적합 |
| starter_templates 생성 시점 | 시드 후 별도 배치 스크립트 | API 키 미확보 상태에서 시드 우선 완료 |
| 스키마 변경 원칙 | 최소 명세 — 명시적 금지만 삭제 | DESIGN_DECISIONS.md §12 지침 |
