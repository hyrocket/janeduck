# JaneDuck 업무일지 — 2026-05-15

## 주요 진행

### CLAUDE.md v3 업데이트
- 폴더 구조 정리: `echo-writing/` → `writing/`, `sentence-writing/` 제거
- Writing Mode 용어 전체 통일 (Echo Writing → Writing Mode)
- Vercel Python Functions 검증 실패 시 대안 3가지 추가 (옵션 A/B/C)
- Phase 4 내용 보강: 세션 시작 화면, PWA 설정, 모바일/태블릿 UI 폴리시
- AI 워크플로우 — LangGraph 활용 전략 섹션 신설

### GitHub 연결
- Remote 추가 및 push 완료: https://github.com/hyrocket/janeduck
- 브랜치: main

### Neon DB 연결 + 마이그레이션
- `.env.local` DATABASE_URL 설정 완료
- `npm run db:migrate` 실행 → 5개 테이블 생성 완료
  - decks, cards, user_cards, writing_attempts, study_sessions
- **버그 수정:** `migrate.ts`가 `;`로 SQL 분리 시 `$$` 트리거 함수 내부 `;`도 잘라내는 문제 → `splitStatements()` 함수로 해결

### Vercel 배포 + Python Functions 검증
- `vercel.json`의 `runtime: "python3.12"` 형식이 Vercel CLI 53.x에서 유효하지 않음 → 제거 후 `.python-version` 파일로 대체
- Internal Server Error 원인 분리: bare handler(표준 라이브러리만)로 테스트 → 함수 자체는 정상 동작 확인
- **Mangum 설정 수정:** `Mangum(app)` → `Mangum(app, lifespan="off")`
- **버전 조회 수정:** `langgraph.__version__` 없음 → `importlib.metadata.version("langgraph")`
- 최종 검증 결과: ✅ `{"status": "ok", "langgraph": "0.2.70"}`

---

## 남은 작업

### Phase 0 잔여 (Phase 1 진입 전 필요)
- [ ] Google OAuth credentials → `AUTH_GOOGLE_ID`, `AUTH_GOOGLE_SECRET`
- [ ] Anthropic API 키 → `ANTHROPIC_API_KEY`
- [ ] Vercel 환경변수 설정 (DATABASE_URL, AUTH_SECRET, AUTH_GOOGLE_ID/SECRET, ANTHROPIC_API_KEY)

### Phase 1 (단어 큐레이션)
- [ ] Sec 1 단어 50개 큐레이션
- [ ] 단어 메타데이터 AI 생성 + 검수
- [ ] 시스템 Deck 2개 시드 데이터 삽입

---

## 주요 결정사항

| 항목 | 결정 | 이유 |
|------|------|------|
| Python Functions 번들 크기 | **현 스택 유지** (307MB / 500MB 이내) | 슬림화 불필요, 여유 있음 |
| Vercel Python runtime 지정 | `vercel.json` runtime 필드 제거 → `.python-version` 파일 | Vercel CLI 53.x 호환성 |
| FastAPI + Mangum 패턴 | `Mangum(app, lifespan="off")` | Vercel 서버리스 환경에서 lifespan 미지원 |
| Writing Mode 용어 | Echo Writing / Sentence Writing 폐기 → **Writing Mode + scaffold_level** 통일 | CLAUDE.md v2에서 이미 설계 완료, 코드 레벨도 통일 필요 |
